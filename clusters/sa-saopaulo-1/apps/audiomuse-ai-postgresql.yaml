---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: audiomuse-ai-postgresql
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: namespaces
    - name: postgresql-cluster
  healthCheckExprs:
    - apiVersion: postgresql.cnpg.io/v1
      kind: Database
      current: >-
        has(status) &&
        has(status.observedGeneration) &&
        status.observedGeneration == metadata.generation &&
        has(status.applied) &&
        status.applied == true
      failed: >-
        has(status) &&
        has(status.observedGeneration) &&
        status.observedGeneration == metadata.generation &&
        has(status.applied) &&
        status.applied == false
    - apiVersion: postgresql.cnpg.io/v1
      kind: DatabaseRole
      current: >-
        has(status) &&
        has(status.observedGeneration) &&
        status.observedGeneration == metadata.generation &&
        has(status.applied) &&
        status.applied == true
      failed: >-
        has(status) &&
        has(status.observedGeneration) &&
        status.observedGeneration == metadata.generation &&
        has(status.applied) &&
        status.applied == false
  interval: 5m0s
  path: ./apps/audiomuse-ai/postgresql
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 2m0s
  wait: true
