apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soju
  labels:
    app.kubernetes.io/name: soju
    app.kubernetes.io/instance: soju
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
