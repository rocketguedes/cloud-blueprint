apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: manage-crds-garage-garage
  labels:
    app.kubernetes.io/name: garage
    app.kubernetes.io/instance: garage
rules:
  - apiGroups:
      - deuxfleurs.fr
    resources:
      - garagenodes
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
